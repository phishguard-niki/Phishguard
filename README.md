# Anti-Scam Guard (v0.4.2)
- 混合模式：啟發式 + 黑名單（165 & 自訂）。
- 強度 C：全畫面確認頁（需按「我已了解」繼續）。
- Options：語言、掃描圖片、短網址提示、是否顯示大貼紙、黑名單編輯。

## 安裝
`chrome://extensions` → Developer mode → Load unpacked → 選擇本資料夾（包含 manifest.json）。
